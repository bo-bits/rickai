# Expo HAS CHANGED

This project is pinned to **Expo SDK 56** (kept Expo Go-compatible — no custom
native modules, since Expo Go is single-SDK and the App Store build runs SDK 56).
Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before
writing any code.
